appId: com.advisorarmor.devicewatch
productName: AdvisorArmor
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
directories:
  output: release
files:
  - out/**
  - package.json
asar: true
mac:
  category: public.app-category.utilities
  identity: null
  target:
    - dmg
    - zip
win:
  target:
    - nsis
publish:
  provider: github
